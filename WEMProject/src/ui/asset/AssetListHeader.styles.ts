import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { display: "flex", flexDirection: "row", alignContent: 'stretch', justifyContent: 'space-between', alignItems: "center"},
});